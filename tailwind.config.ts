import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        delicatus: ["Delicatus", "sans-serif"],
      },
    },
  },
};

export default config;
